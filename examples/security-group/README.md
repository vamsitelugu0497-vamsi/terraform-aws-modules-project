# Security Group Example

This example demonstrates how to create an AWS Security Group using the reusable module.

## Prerequisites

- An existing VPC
- Update `terraform.tfvars` with the VPC ID

## Run

```bash
terraform init
terraform plan
terraform apply
```
