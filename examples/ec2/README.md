# EC2 Example

This example demonstrates how to launch an Amazon EC2 instance using the reusable EC2 module.

## Prerequisites

- Existing VPC
- Existing subnet
- Existing Security Group
- Existing EC2 Key Pair

## Run

```bash
terraform init
terraform plan
terraform apply
```
